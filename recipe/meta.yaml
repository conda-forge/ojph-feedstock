{% set version = "0.2.0" %}

package:
  name: ojph
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/o/ojph/ojph-{{ version }}.tar.gz
  sha256: 3602fdd8c4e960f6b8c1c1e00b7a0d405c62e7c6780b115ab5c4e69038bbca14

build:
  script: python -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
  host:
    - python
    - pip
    - setuptools
    - pybind11
    - openjph
  run:
    - python
    - numpy >=1.24

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - ojph

about:
  home: https://github.com/ramonaoptics/ojph
  summary: 'Python bindings for OpenJPH, a JPEG 2000 codec'
  license: BSD-3-Clause
  license_file: LICENSE.txt
  dev_url: https://github.com/ramonaoptics/ojph

extra:
  recipe-maintainers:
    - hmaarrfk
